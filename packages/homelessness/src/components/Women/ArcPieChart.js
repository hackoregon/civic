import React from 'react';
import PropTypes from 'prop-types';

const ArcPieChart = (data) => (
  <div />
)

ArcPieChart.propTypes = {
  data: PropTypes.array.isRequired,
};

export default ArcPieChart;
